# WiFi Audit GUI has NO Python package dependencies - it uses only the standard
# library (tkinter) plus external CLI tools you install with apt.
#
# System prerequisites (Kali/Debian):
#   sudo apt update
#   sudo apt install -y python3-tk aircrack-ng iw iproute2 hcxtools
#
# aircrack-ng provides: airmon-ng, airodump-ng, aireplay-ng, aircrack-ng
# hcxtools provides:    hcxpcapngtool (optional - only for hashcat 22000 export)
